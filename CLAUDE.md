# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Repository status

This repository is currently empty — no source files, build configuration, or documentation have been committed yet. There is no codebase to describe.

Once code is added, replace this placeholder with:

- Commands for build, lint, test (including how to run a single test)
- High-level architecture spanning multiple files
- Important conventions from any README, `.cursor/rules/`, `.cursorrules`, or `.github/copilot-instructions.md`

Until then, treat tasks here as greenfield work and ask the user for stack, tooling, and conventions before assuming any.
